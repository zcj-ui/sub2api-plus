interface:
  display_name: "Sub2API Plus Admin"
  short_description: "Manage Sub2API Plus admin accounts and settings"
  default_prompt: "Use $sub2api-admin to inspect and manage Sub2API Plus backend accounts safely."

policy:
  allow_implicit_invocation: true
