# Sub2API Plus 使用、部署与风险免责声明

版本：2026-08-15

请在下载、构建、配置、部署、升级、运营或向任何用户开放 Sub2API Plus 前完整阅读本文件。本文件用于集中说明当前版本的技术成熟度、运行风险和责任边界，并与仓库中的 [LICENSE](LICENSE)、[NOTICE](NOTICE)、[安全策略](SECURITY.md)及[部署与运营合规承诺](docs/legal/admin-compliance.zh.md)一并阅读。

本文件是风险告知和运维提示，不是对 GNU LGPL-3.0-or-later 的补充许可条件，不限制许可证已经授予的复制、修改、研究和分发权利，也不替代许可证正文。许可证正文与本文件出现不一致时，以许可证正文为准。依法不得排除或限制的责任，依适用法律处理。

## 1. 当前版本不面向生产环境

Sub2API Plus 当前 `0.2.x` 系列属于技术预览和兼容性验证版本，仅适合源码研究、本地开发、隔离测试、接口联调和受控验收。它尚未完成独立第三方安全审计、持续高并发压力测试、故障注入、灾难恢复演练、跨版本迁移与回滚验证、长期数据一致性验证、多区域容灾验证或生产服务等级评估。

不要把当前版本直接用于生产环境、关键业务、公共商业服务、真实付费用户、不可替代数据、高价值账号、核心凭据或需要明确可用性承诺的工作负载。`release` 构建、`latest` 镜像、版本标签、构建成功、测试通过、GitHub Release 或“稳定通道”等字样只描述构建和分发方式，不代表生产认证、安全认证、合规批准、持续维护承诺或适合特定业务。

任何继续评估当前版本的部署者，都应使用隔离网络、合成或可随时撤销的凭据、可丢弃账户和非关键数据，并先完成自己的代码审查、依赖审计、压力测试、备份恢复、迁移回滚、权限检查、出口核验和异常场景验收。测试通过只能证明已覆盖场景在特定环境下的结果，不代表真实网络、第三方代理、不同地区或未来上游版本会得到相同结果。

## 2. 软件按现状提供

本项目依照开源许可证按现状提供。除适用法律另有强制规定外，项目维护者、著作权人和贡献者不对软件无缺陷、不中断、实时更新、数据不丢失、结果准确、协议完全兼容、适销性、特定用途适用性或对任何第三方系统的持续可用性作出保证。

维护者可能根据时间和资源处理 Issue、兼容性变化与安全报告，但不承诺响应时间、修复时间、版本支持期限、上游适配期限、现场运维、账户恢复、数据恢复或服务赔偿。文档、示例、默认值、测试和社区回复是一般技术信息，部署者仍需结合自己的环境独立验证。

## 3. 上游服务和第三方中转

OpenAI、Anthropic、Google、xAI 及其他模型服务商、反向代理、中转商或网络服务商可以随时改变接口路径、请求字段、响应结构、状态码、模型名称、计费方法、配额策略、地区政策、风控规则和服务条款。本项目的兼容层来自有限样本和公开行为，不保证覆盖所有官方、非官方或二次反代实现。

部署者负责确认其账号、API Key、OAuth 凭据、订阅、代理和使用方式符合对应服务提供方的规则及适用要求。项目中的协议转换、客户端特征、会话标识、设备标识、请求头兼容和历史消息处理只是技术兼容机制，不构成任何第三方的许可、授权、认可或背书，也不保证账号不会受到限制、验证、暂停或终止。

第三方中转返回的成功状态、模型名称、余额、用量或错误信息可能被缓存、改写或省略。对重要结果应同时核对原始响应、上游控制台和实际账单，不应只依据本项目界面作出收费、结算、授信、封号、删除账户或容量采购决定。

## 4. 凭据、账户和固定代理

系统可能处理 API Key、OAuth 访问令牌、刷新令牌、Cookie、代理认证、账户标识、订阅信息、额度快照以及请求元数据。错误日志、调试输出、浏览器插件、反向代理、监控平台、备份文件、截图、账户导出、工单或聊天记录都可能泄露这些信息。

部署者应采用独立密钥管理、强管理员认证、多因素认证、最小权限、静态与传输加密、访问审计、日志脱敏、定期轮换、撤销机制和受控备份。不得把真实凭据提交到 Git、Issue、Pull Request、公开网盘或未经批准的诊断平台。凭据一旦出现在公开记录中，应立即撤销并轮换；删除帖子或清理 Git 历史不能使已经泄露的凭据恢复安全。

OpenAI/Codex 账户的固定代理约束用于降低意外出口漂移，但不保证代理服务商可信、匿名、独占、位于标注地区、持续在线或不记录流量。代理故障、DNS 行为、认证错误、连接复用、WebSocket 断开和代理商路由变化可能导致失败、延迟、重复请求或上游风控。部署者必须分别验证模型请求、额度查询、账户测活、OAuth 刷新和 WebSocket 是否经过预期出口。

## 5. 额度、积分与参考金额

`GET /backend-api/wham/usage`、`credits.balance`、重置次数、5 小时与 7 天窗口以及其他额度信息，均是上游在特定查询时刻提供的快照。网络缓存、账户切换、代理缓存、字段变更、上游延迟、解析失败、批量任务超时或数据库写入失败都可能使页面数据过期、不完整或与上游控制台不一致。

前端按 `Credit / 25` 计算的美元数字仅用于界面比较和人工参考。它不是法定货币余额、可提现资产、承诺售价、发票、支付凭证、会计记录、对账结果或结算依据。不得用该参考值自动付款、自动授信、确认收入、计算税款，或向第三方保证可使用金额。

“有积分时不自动 429”“两次明确 429 后冷却”和本地额度阈值处理只改变本项目内部的调度与状态策略，不会增加上游余额、解除上游限制或保证下一次请求成功。429 也可能来自代理、中转、区域、模型、组织、并发或上游全局策略。操作人员应保留脱敏后的原始状态和响应类别，结合上游控制台判断原因。

## 6. 合成工具历史和协议转换

OpenAI/Codex 的“卡429”功能可能在满足条件的非工具调用消息历史末尾追加合成工具调用及成对响应。该内容会进入发送给模型的上下文，并可能被 Agent 看到。虽然实现会排除 Claude Code、compact、真实工具续链和指定模型分支，未来客户端格式、反代改写或上游行为变化仍可能使识别结果不同。

协议转换可能删除、补充、重排或规范化字段；流式与非流式响应之间的桥接可能改变错误出现的时点；连接中断后的重试可能造成重复提交、重复工具动作或重复计费。涉及有副作用的工具时，调用方必须使用幂等键、人工确认、事务边界和重复检测，不能仅依赖网关判断一次请求是否已经执行。

## 7. 调度、并发和账户状态

调度器会根据已观察到的并发、健康状态、额度快照和配置优先复用少量账户，并在容量不足时扩展账户。所有这些信号都可能因为缓存、进程重启、多实例不同步、请求超时、客户端断开、数据库延迟或上游响应变化而滞后。

调度结果不是严格公平队列、财务限额、容量保证、账户寿命保证或防止超卖的独立控制面。高并发下仍可能出现短暂超额、选择到刚刚失效的账户、重复重试或多个实例同时使用同一账户。需要明确预算或容量边界的部署，必须在外部增加速率限制、并发上限、预算控制、熔断、队列和告警，并验证多实例共享状态。

测活结果只代表某个探针在某一时间点的结果。失败池、重置次数和额度查询可能因网络、代理或上游短时异常产生误判。自动禁用、删除或替换账户前应人工复核，并保留恢复路径。不要把测活成功视为未来请求成功、账号长期有效或服务商认可该使用方式的证明。

## 8. 批量盘点、导入与编辑

一键盘点、批量测活、批量编辑、API Key 更新和账户导入会同时读取或修改多个账户。选择范围错误、旧页面提交、重复点击、任务超时、部分事务失败、字段兼容差异或并发修改可能造成部分成功、部分失败、覆盖新数据或产生短暂不一致。

执行批量操作前，应记录选中的账户范围，导出经过加密和脱敏的可恢复备份，先在少量测试账户验证，并在完成后逐项核对成功、失败和跳过结果。账户导出属于高敏感数据，不应通过公共网盘、群聊、Issue、PR 或普通邮件传输。导入来源和格式必须经过审查；不要假设旧版本、第三方 Fork 或其他工具生成的每个字段都与当前版本语义一致。

## 9. 数据库、缓存、备份与恢复

项目不承诺数据库迁移可逆，也不保证新版本写入的数据能被旧版本完整读取。PostgreSQL 与 Redis 可能在不同时间写入状态，只复制容器镜像、可执行文件或数据库目录不等于获得一致备份。加密密钥丢失时，即使数据库仍在，也可能无法恢复受保护内容。

部署者应按同一恢复点保存 PostgreSQL、需要保留的 Redis 状态、配置、加密密钥、代理配置、附件和对应版本源码，并在独立环境实际执行恢复演练。升级前固定版本和镜像摘要，核对迁移说明，准备停机窗口与回滚判据。不要在未验证备份可恢复的情况下使用滚动标签自动升级。

## 10. 网络与管理端暴露

示例 Compose、示例环境变量、本地端口和默认拓扑用于开发和验收，不是经过加固的生产配置。把管理端直接暴露到公网、使用弱密码、共享管理员账号、开放 PostgreSQL 或 Redis、信任任意转发头、允许不受控 HTTP 上游、错误配置 CORS 或复用示例密钥都会增加接管、伪造来源、数据泄露和中间人攻击风险。

隔离测试环境也应替换所有示例秘密，使用 HTTPS、可信反向代理、访问控制、防火墙、最小权限数据库用户、速率限制、日志轮换和安全更新。管理接口与模型 API 应按用途分离暴露，内部依赖不应直接对公网监听。对每次网络或代理配置变化，都应重新验证真实出口、客户端 IP 解析和管理端访问边界。

## 11. 隐私、内容和模型输出

模型输入、输出、附件、工具参数、错误信息和日志可能包含个人信息、商业秘密、受保护作品或其他敏感数据。本软件不判断上传者是否具备处理权利，也不自动替部署者完成隐私告知、同意管理、数据最小化、跨境评估、保留期限、访问更正、删除请求、内容审核或数据处理协议。

模型输出可能错误、不完整、过时、有偏见或不适合特定场景。不要把模型输出作为无人监督的医疗、法律、金融、招聘、信用、安全控制、基础设施控制或其他高影响决定的唯一依据。涉及外部动作、资金、权限、文件删除或生产变更时，应加入人工复核、最小权限和可回滚机制。

## 12. 更新、依赖和供应链

在线更新、容器基础镜像、系统包、Go module、npm 包、构建服务、GitHub Actions 和第三方下载地址都可能引入供应链风险。校验和可以帮助发现下载内容变化，但不能替代发布来源核验、签名验证、源码审查、依赖锁定、漏洞扫描、构建隔离或可复现构建。

自动更新可能同时带来数据库迁移、配置默认值变化、接口行为变化和不可逆的数据格式变化。评估环境也应先在副本上验证升级，保留旧镜像、对应源码、配置和恢复点。任何非本仓库发布的镜像、二进制、脚本、Fork、插件或打包版本均由其提供者负责，本项目对第三方修改内容、可用性或更新源不作保证。

## 13. 名称、关联与第三方运营

Sub2API Plus 是 [Wei-Shaw/sub2api](https://github.com/Wei-Shaw/sub2api) 的修改发行版，不是上游官方发行版。第三方实例、商业服务、收费套餐、宣传页面、客户支持或运营活动由相应部署和运营主体独立实施。开源、提交代码、维护文档、发布版本、回复 Issue 或提供一般技术说明，不表示项目、上游作者、贡献者或维护者参与、授权、认可、担保或背书第三方业务。

部署者不得用项目名称、标识、截图、文档或仓库链接使用户误认为其服务由上游或本项目官方运营。修改和分发时应保留 `LICENSE`、`NOTICE`、对应源码与构建信息，清楚标注自己的修改、服务主体、支持渠道和隐私政策。

## 14. 使用前最低检查清单

在任何隔离测试开始前，至少确认：已阅读许可证和本文件；使用可撤销的测试凭据；全部示例密码和加密密钥已经替换；PostgreSQL 与 Redis 未暴露公网；管理端已限制访问；日志不会记录完整 Token；代理出口已分别验证；账户与导入文件已有加密备份；上游条款和费用已经核对；批量操作先经过小范围验证；升级和数据库恢复已经演练；异常扣费、凭据泄露、代理漂移、持续 429、数据错乱或权限越界时有明确的停止流程。

该清单只是最低提示，并不穷尽所有技术、运营、合同、税务、隐私、安全和监管要求。部署者应根据实际业务、用户范围、数据类别、所在地区和上游服务自行增加控制措施，并在需要时咨询相应专业人员。

## 15. 文件关系

- [LICENSE](LICENSE)：适用的 GNU LGPL-3.0-or-later 许可证正文。
- [NOTICE](NOTICE)：上游来源、修改发行版和版权归属说明。
- [SECURITY.md](SECURITY.md)：漏洞报告方式和敏感信息处理要求。
- [deploy/README.md](deploy/README.md)：测试部署方式及配置注意事项。
- [中文部署与运营合规承诺](docs/legal/admin-compliance.zh.md)：管理控制台展示的运营确认文本。
- [English deployment and operation commitment](docs/legal/admin-compliance.en.md)：英文运营确认文本。

使用者应以仓库当前提交中的文件为准。复制、打包或再次分发时，请让本文件、许可证、来源说明和对应版本源码保持可见，避免只分发可执行文件或镜像而遗漏风险信息及许可证材料。
